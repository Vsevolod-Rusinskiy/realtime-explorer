# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Скопируйте этот файл в .env.local и настройте под ваше окружение

# -----------------------------------------------------------------------------
# GRAPHQL API ENDPOINTS
# -----------------------------------------------------------------------------
# Hasura GraphQL endpoints for data fetching and subscriptions
NEXT_PUBLIC_HASURA_GRAPHQL_HTTP=http://localhost:3003/v1/graphql
NEXT_PUBLIC_HASURA_GRAPHQL_WS=ws://localhost:3003/v1/graphql

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------
# WARNING: This variable has SECURITY ISSUE!
# NEXT_PUBLIC_ prefix makes it visible in browser to all users
# TODO: Move to server-side authentication
NEXT_PUBLIC_HASURA_ADMIN_SECRET=your_hasura_admin_secret

# -----------------------------------------------------------------------------
# PRODUCTION ENDPOINTS (for reference)
# -----------------------------------------------------------------------------
# For production deployment, use your actual GraphQL endpoints:
# NEXT_PUBLIC_HASURA_GRAPHQL_HTTP=https://your-domain.com/v1/graphql  
# NEXT_PUBLIC_HASURA_GRAPHQL_WS=wss://your-domain.com/v1/graphql
