# =============================================================================
# SQUID INDEXER CONFIGURATION
# =============================================================================
# Скопируйте этот файл в .env и настройте под ваше окружение

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL database connection settings
DB_HOST=localhost                    # Database host
DB_PORT=5432                        # Database port  
DB_NAME=postgres                    # Database name
DB_USER=postgres                    # Database username
DB_PASS=your_secure_password        # Database password

# -----------------------------------------------------------------------------
# GRAPHQL SERVER CONFIGURATION  
# -----------------------------------------------------------------------------
# GraphQL server settings for client connections
GQL_HOST=localhost                  # GraphQL server host
GQL_PORT=4350                      # GraphQL server port
GQL_PROTO=ws                       # GraphQL protocol (ws/wss)

# -----------------------------------------------------------------------------
# BLOCKCHAIN RPC CONFIGURATION
# -----------------------------------------------------------------------------
# RPC endpoint is currently hardcoded in src/config.ts
# Consider moving to environment variable for production
# RPC_URL=wss://test.qfnetwork.xyz/wss

# -----------------------------------------------------------------------------
# PROCESSING CONFIGURATION
# -----------------------------------------------------------------------------
# Block processing settings (currently hardcoded in src/config.ts)
# MAX_BLOCKS=1000                   # Maximum blocks to keep in database
# DEPTH=50                          # Number of blocks to process from chain tip
